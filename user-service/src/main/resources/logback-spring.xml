<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- ===================== -->
    <!--  COMMON PROPERTIES    -->
    <!-- ===================== -->
    <property name="LOG_PATH" value="logs"/>
    <property name="APP_NAME" value="user-service"/>

    <!-- ===================== -->
    <!--  CONSOLE APPENDER     -->
    <!-- ===================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [${APP_NAME}] [%thread]%replace( [traceId=%X{traceId}, spanId=%X{spanId}]){'traceId=, spanId=',''} %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!--  FILE APPENDER (ALL)  -->
    <!-- ===================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-%d{yyyy-MM-dd}-%i.log.gz</fileNamePattern>
            <maxFileSize>50MB</maxFileSize>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [${APP_NAME}] [%thread]%replace( [traceId=%X{traceId}, spanId=%X{spanId}]){'traceId=, spanId=',''} %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!--  ERROR FILE APPENDER  -->
    <!-- ===================== -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${APP_NAME}-error.log</file>

        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${APP_NAME}-error-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>60</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [${APP_NAME}] [%thread]%replace( [traceId=%X{traceId}, spanId=%X{spanId}]){'traceId=, spanId=',''} %logger{36} : %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===================== -->
    <!--  ROOT LOGGER          -->
    <!-- ===================== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

</configuration>